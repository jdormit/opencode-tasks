---
description: Schedule a recurring prompt that fires inside this session (e.g. `/loop 5m check the deploy`). Requires the opencode-tasks plugin.
---
The opencode-tasks plugin handles /loop. If you're seeing this message instead of a confirmation, the plugin isn't loaded — add `"opencode-tasks"` to the `plugin` array in your opencode config. The user typed: $ARGUMENTS
