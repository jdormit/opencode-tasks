---
description: List the active /loop schedules for this session.
---
The opencode-tasks plugin handles /loop-list. If you're seeing this message instead of a list, the plugin isn't loaded — add `"opencode-tasks"` to the `plugin` array in your opencode config.
