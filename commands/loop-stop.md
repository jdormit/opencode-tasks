---
description: Stop loops started with /loop. Pass an id to stop just one, or no arguments to stop every loop in this session.
---
The opencode-tasks plugin handles /loop-stop. If you're seeing this message instead of a confirmation, the plugin isn't loaded — add `"opencode-tasks"` to the `plugin` array in your opencode config. The user typed: $ARGUMENTS
